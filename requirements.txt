# Prairie Genomics Suite - Streamlit Cloud Requirements
# Minimal requirements for successful deployment

# Core web framework
streamlit>=1.28.0

# Essential data libraries
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0

# Visualization (essential)
plotly>=5.17.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional - Bioinformatics (graceful fallback if not available)
scikit-learn>=1.3.0
lifelines>=0.27.0

# Optional - Gene conversion (graceful fallback if not available)
mygene>=3.2.2

# File handling
openpyxl>=3.1.0

# Progress bars and utilities
tqdm>=4.66.0

# Note: Advanced features like R integration, literature search, 
# and some bioinformatics packages are made optional with graceful fallbacks
# to ensure the app works even if these aren't available in the cloud environment
